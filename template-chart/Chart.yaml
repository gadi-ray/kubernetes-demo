apiVersion: v2
name: template-chart
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
type: application

# This is the chart version. AKA infa version
version: 0.1.0

# This is the version number of the application being deployed.
appVersion: "1.20.0"
